import '../constants/constants.dart';

class PatientDataHandler {
  // DOING the data handling in the Patient class on its own for now, although it works more as a struct
  // Might move here in the future if required
}
